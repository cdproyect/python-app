{{- if and (eq .Values.serviceType "NodePort") .Values.nodePort.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-istio
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
    gateway.istio.io/managed: istio.io-gateway-controller
    gateway.networking.k8s.io/gateway-name: {{ .Values.name }}
    istio.io/gateway-name: {{ .Values.name }}
  annotations:
    networking.istio.io/service-type: NodePort
spec:
  type: NodePort
  selector:
    gateway.networking.k8s.io/gateway-name: {{ .Values.name }}
  ports:
    - appProtocol: tcp
      name: status-port
      nodePort: {{ .Values.nodePort.statusPort }}
      port: 15021
      protocol: TCP
      targetPort: 15021
    - appProtocol: http
      name: http
      nodePort: {{ .Values.nodePort.http }}
      port: 80
      protocol: TCP
      targetPort: 80
    {{- if .Values.https.enabled }}
    - appProtocol: https
      name: https
      nodePort: {{ .Values.nodePort.https }}
      port: 443
      protocol: TCP
      targetPort: 443
    {{- end }}
{{- end }}